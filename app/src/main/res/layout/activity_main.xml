<?xml version="1.0" encoding="utf-8"?>
<!--

IMPORTANTE:
- Este archivo necesita que tengáis el archivo navegador.xml en res/navigation/
- Si no existe, Android Studio os dará error de compilación
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <!-- tools:context ayuda al editor a saber qué Activity usa este layout -->

    <!--
    NavHostFragment: Contenedor que gestiona la navegación entre fragments

    ¿Qué hace cada atributo?
    - android:name: especifica la clase NavHostFragment
    - app:navGraph: referencia al grafo de navegación (rutas entre pantallas)
    - app:defaultNavHost: permite que el botón atrás funcione automáticamente
    -->
<androidx.fragment.app.FragmentContainerView
android:id="@+id/nav_host_fragment"
android:name="androidx.navigation.fragment.NavHostFragment"
android:layout_width="0dp"
android:layout_height="0dp"
app:layout_constraintTop_toTopOf="parent"
app:layout_constraintBottom_toBottomOf="parent"
app:layout_constraintStart_toStartOf="parent"
app:layout_constraintEnd_toEndOf="parent"
app:navGraph="@navigation/navegador"
app:defaultNavHost="true" />

    <!--
    NOTA:
    Si Android Studio os da error en app:navGraph="@navigation/nav_graph"
    es porque aún no habéis creado el archivo de navegación.

    Para crearlo:
    1. Clic derecho en res > New > Android Resource File
    2. File name: nav_graph
    3. Resource type: Navigation
    4. Click OK
    5. Añadir vuestros fragments al grafo visual
    -->

    </androidx.constraintlayout.widget.ConstraintLayout>